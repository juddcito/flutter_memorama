name: flutter_memorama
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  animate_do: ^3.1.2
  flip_card: ^0.7.0
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  intl: ^0.18.1
  path: ^1.8.3
  sqflite: ^2.3.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true 
  image_path: "assets/grape.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
