

import 'package:flutter/material.dart';

class GameScreen extends StatelessWidget {

  final int numPares;

  GameScreen({
    super.key,
    required this.numPares
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}